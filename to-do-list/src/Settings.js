import { Form } from "react-router-dom";

function Settings() {
  return (
    <div className="page Settings">
      <h1>
        Settings
      </h1>
    </div>
  );
}

export default Settings;